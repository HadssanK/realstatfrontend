import RegisterPage from "@/components/auth/RegisterPage";

export const metadata = {
  title: "Register — PropFind",
  description: "Create your PropFind account to post properties, manage inquiries and save favourites.",
};

export default function RegisterRoute() {
  return <RegisterPage />;
}
