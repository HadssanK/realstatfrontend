import AddPropertyPage from "@/components/dashboard/add/AddPropertyPage";

export const metadata = {
  title: "Add Property — PropFind Dashboard",
  description: "Post a new property listing on PropFind.",
};

export default function AddPropertyRoute() {
  return <AddPropertyPage />;
}
