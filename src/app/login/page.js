import LoginPage from "@/components/auth/LoginPage";

export const metadata = {
  title: "Login — PropFind",
  description: "Login to access your PropFind dashboard, saved properties and inquiries.",
};

export default function LoginRoute() {
  return <LoginPage />;
}
